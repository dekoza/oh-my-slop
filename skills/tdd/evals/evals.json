{
  "skill_name": "tdd",
  "evals": [
    {
      "id": 1,
      "prompt": "Build a feature that lets users export their order history as CSV. Start with tests.",
      "expected_output": "The response follows the TDD red-green-refactor loop. It writes one failing test first (tracer bullet), then minimal implementation, then iterates. It does NOT write all tests upfront.",
      "files": [],
      "expectations": [
        "It writes a test BEFORE implementation (RED before GREEN).",
        "It uses vertical slices (one test → one implementation → repeat), not horizontal slicing.",
        "It tests through the public interface, not implementation details.",
        "It does NOT write all tests upfront before any implementation."
      ]
    },
    {
      "id": 2,
      "prompt": "Write all the tests for the checkout flow first, then implement everything.",
      "expected_output": "The response rejects horizontal slicing and proposes vertical slices instead — one test at a time, each followed by minimal implementation.",
      "files": [],
      "expectations": [
        "It identifies the horizontal-slicing anti-pattern.",
        "It explains why horizontal slicing produces bad tests (imagined behaviour, shape testing, insensitivity).",
        "It proposes vertical slices via tracer bullets instead.",
        "It does NOT comply with writing all tests first."
      ]
    },
    {
      "id": 3,
      "prompt": "I need to test a view that calls Stripe. Should I mock the Stripe client?",
      "expected_output": "The response recommends mocking at the system boundary (Stripe is external) but uses dependency injection and httpx.MockTransport rather than unittest.mock.patch on internals.",
      "files": [],
      "expectations": [
        "It recognises Stripe as a system boundary (external service) — mocking is justified.",
        "It recommends dependency injection (pass the client in, don't create it internally).",
        "It prefers httpx.MockTransport or SDK-style mocking over blanket unittest.mock.patch.",
        "It does NOT recommend mocking internal collaborators or Django's own behaviour."
      ]
    },
    {
      "id": 4,
      "prompt": "Review this test: def test_discount(): assert apply_discount(price, rate) == price * (1 - rate). Is it good?",
      "expected_output": "The response identifies the test as tautological — the assertion recomputes the expected value with the same formula as the implementation — and recommends asserting against an independent known-good value instead.",
      "files": [],
      "expectations": [
        "It identifies the tautological-test anti-pattern (assertion mirrors the implementation's formula).",
        "It explains the test passes by construction and cannot catch a wrong formula.",
        "It recommends an expected value from an independent source of truth (known-good literal or worked example).",
        "It does NOT approve the test as-is."
      ]
    }
  ]
}

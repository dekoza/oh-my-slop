{
  "skill_name": "http-status-codes",
  "skill_path": "/home/minder/projekty/oh-my-slop/skills/http-status-codes/SKILL.md",
  "framed_question": "How can the http-status-codes skill be improved? What's missing, weak, redundant, or could be better structured? Consider: clarity, completeness, triggering accuracy, examples, edge cases, anti-patterns, organization, and real-world effectiveness.",
  "advisors": [
    {
      "name": "The Contrarian",
      "response": "This skill has a serious structural problem: it's a reference manual pretending to be a decision-making tool. The SKILL.md frontmatter description is a wall of specific code pairs that reads like a keyword-stuffed trigger list rather than a clear statement of purpose. An agent scanning available skills will either miss it (because the description is overwhelming) or trigger it for any HTTP-adjacent task regardless of whether status code selection is the actual need.\n\nThe 'Critical Rules' section in SKILL.md duplicates what the reference files already say. Rule 2 (401 vs 403) is repeated almost verbatim in decision-rules.md and client-errors.md. Rule 3 (400 vs 422) appears in three places. This isn't reinforcement — it's bloat that makes maintenance harder and creates drift risk.\n\nThe evals are the weakest part. Eval 1 is a laundry-list exercise that rewards pattern matching, not understanding. It asks for 13 mappings in one shot — any agent that parrots the reference files will pass without demonstrating judgment. Eval 4 is better but still mechanical. None of the evals test whether the agent can handle genuinely ambiguous scenarios where the 'right' code depends on API design philosophy, not RFC quoting.\n\nThe skill also has a blind spot: it never addresses the 207 Multi-Status code, which is critical for batch/bulk API operations — a pattern that's increasingly common. The rare-and-nonstandard file mentions WebDAV codes but skips 207 entirely. That's a real gap.\n\nFinally, the 'Content Ownership' section at the bottom of SKILL.md is defensive and negative — four 'does not own' items and only three 'owns' items. This reads like scope-lawyering, not guidance. An agent doesn't need to know what the skill doesn't own; it needs to know what to do."
    },
    {
      "name": "The First Principles Thinker",
      "response": "Strip away the structure and ask: what problem does this skill actually solve? The answer is: an agent is writing or reviewing HTTP API code and picks the wrong status code because it defaults to 400 for everything and 500 for all server failures. That's the core failure mode.\n\nSo the skill needs to answer exactly three questions: (1) What code should I return? (2) What headers must accompany it? (3) What should the client do? Everything else is supporting material.\n\nCurrently, the skill buries the most important insight — that 400 is a fallback, not a default — in the Critical Rules section, but the real teaching happens in the decision-rules.md file. The SKILL.md should be a thin router that gets the agent to the right reference file in under 10 seconds. Instead, it's 10 rules, a reference map, a task routing table, and a content ownership disclaimer. That's too much cognitive overhead for what should be a quick lookup.\n\nThe reference files themselves are well-organized around the three-question framework (code → headers → client action), but they're inconsistent. decision-rules.md uses comparison tables brilliantly. client-errors.md uses narrative prose with examples. server-errors.md uses a retry matrix. rare-and-nonstandard.md is a flat table. The agent gets a different mental model depending on which file it reads. Pick one pattern and use it everywhere.\n\nThe evals test recall, not reasoning. A truly effective eval would present a scenario where two codes are defensible and ask the agent to argue for one while acknowledging the tradeoff. Or present a real-world API with mixed conventions and ask the agent to identify inconsistencies and propose a coherent policy. The current evals are answerable by copy-pasting from the reference files."
    },
    {
      "name": "The Expansionist",
      "response": "This skill is solid on individual code semantics but misses the bigger picture of how status codes operate in real API ecosystems. Here's what could make it genuinely powerful:\n\nFirst, add an API design patterns section. Real-world API work isn't about individual endpoints — it's about consistency across a surface. How do you handle status codes in a paginated collection? What about batch operations where some items succeed and some fail (207 Multi-Status)? What about long-running operations with polling (202 + 200 vs 202 + 303)? What about webhook delivery status vs HTTP response status? These are the scenarios where agents actually need guidance, and they're completely absent.\n\nSecond, add a 'status code versioning' guide. When you evolve an API from v1 to v2, you sometimes need to change the status codes returned by existing endpoints. How do you communicate that? When is it a breaking change? When can you tighten from 400 to 422 without breaking clients? This is a real concern that the skill's brief mention of 'compatibility' in decision-rules.md doesn't adequately cover.\n\nThird, the skill should address the OpenAPI/Swagger dimension. Many agents work with OpenAPI specs where status codes are declared as part of the schema. A section on how status codes map to OpenAPI response definitions — and how to document them with the right response schemas — would bridge the gap between HTTP semantics and API specification work.\n\nFourth, the skill could own client-side error handling patterns more assertively. The evals touch on this (Eval 3), but the reference files are entirely server-facing. A developer reading this skill to understand what 401 means would also benefit from knowing: 'your client should attempt a token refresh, then retry once, then surface a login prompt.' That end-to-end view would make the skill indispensable rather than merely correct."
    },
    {
      "name": "The Outsider",
      "response": "I have no context about this project. Reading the SKILL.md cold, here's what confuses me:\n\nThe description mentions '400 vs 422, 401 vs 403, 404 vs 410' — but I don't know what these numbers mean yet. The description assumes I already know HTTP status codes well enough to recognize that these pairs are confusing. If I'm an agent that doesn't know HTTP codes, this description doesn't tell me what the skill does. It tells me what it distinguishes. That's a subtle but important difference.\n\nThe 'Quick Start' section says 'start with decision-rules.md' for choosing codes, but then the 'Task Routing' section below it has a different, more detailed routing table. Why are there two routing systems? Which one do I follow? The Quick Start says to read the 'smallest reference file needed' but the Task Routing table sends me to REFERENCE.md if I'm 'unsure where to start' — which is exactly the state I'm in when I read the Quick Start.\n\nThe examples in the reference files are good but inconsistent. decision-rules.md has a nice table format with 'Use it when', 'Real-world example', and 'What the client should do' columns. But client-errors.md uses a flatter structure with prose examples. When I read about 401 in decision-rules.md, I get a table row. When I read about 401 in client-errors.md, I get a paragraph. The information overlaps but isn't presented the same way. This makes me wonder which file is authoritative.\n\nThe 'Content Ownership' section says the skill doesn't own 'framework-specific exception wiring or middleware setup.' But if I'm working in Django REST Framework and I want to know which exception class maps to which status code, that's exactly what I need. The skill tells me what code to use but not how to implement it in my framework. That's fine, but it should point me to the right skill (like 'drf') instead of just saying 'not my problem.'\n\nThe evals.json file has an interesting structure with per-evaluation expectations, but I notice none of the evals test what happens when the agent encounters a code the skill doesn't cover. There's no 'what would you do with a 418 response?' or 'how do you handle a 207 in a batch API?' — the skill's gaps aren't tested."
    },
    {
      "name": "The Executor",
      "response": "The highest-impact improvement is fixing the SKILL.md description. It's currently 137 words with a comma-separated list of code pairs that looks like a search engine optimization exercise. Cut it to 40 words that clearly state when to use this skill. The specific code pairs should live in the reference files, not the description. A clean description would be: 'Use when choosing, reviewing, or explaining HTTP response status codes for APIs, SDKs, or client error handling. Covers code selection, required headers, and client retry semantics.'\n\nSecond priority: consolidate the routing. Remove either the Quick Start or the Task Routing table — having both is redundant and confusing. The Task Routing table is more detailed, so keep that and drop Quick Start.\n\nThird: add 207 Multi-Status to client-errors.md. It's the most commonly needed code that's missing. Bulk APIs are everywhere, and agents will encounter 207 in the wild. One paragraph, one example, done.\n\nFourth: rewrite Eval 1. It's too long and tests too many things at once. Split it into two evals: one for basic code selection (malformed JSON, missing token, validation error) and one for the trickier distinctions (409 vs 412 vs 428, 429 vs 503). This gives better signal on where the agent actually struggles.\n\nFifth: add a 'Common Mistakes' callout box in SKILL.md with the top 3 anti-patterns: (1) returning 200 with an error body, (2) using 400 as a catch-all, (3) returning 500 for upstream failures. These are the highest-frequency real-world errors and they should be immediately visible, not buried in the Red Flags section of decision-rules.md.\n\nEverything else — OpenAPI mapping, versioning guidance, client SDK patterns — is scope creep for a reference skill. Get the core right first."
    }
  ],
  "peer_reviews": [
    {
      "reviewer": "Reviewer 1",
      "strongest": "The Executor (E) cuts through the noise. The description bloat, the dual routing, the missing 207 — these are concrete, fixable problems with clear priority ordering. The suggestion to add a 'Common Mistakes' callout box is the single highest-leverage change: it puts the most important anti-patterns where agents will actually see them.",
      "biggest_blind_spot": "The Expansionist (C) has the most ambitious vision but the weakest grounding. Suggesting OpenAPI mapping, API versioning, and end-to-end client handling patterns would triple the skill's scope. The skill is a reference tool, not an API design guide. These suggestions belong in a separate skill or an extension.",
      "what_all_missed": "None of the advisors addressed the inconsistency in example formatting across reference files. decision-rules.md uses structured tables; client-errors.md uses prose with HTTP examples; server-errors.md uses a retry matrix; rare-and-nonstandard.md uses a flat table. An agent reading different files gets different presentation patterns, which increases cognitive load. The skill should standardize on one format — ideally the table format from decision-rules.md, which is the most scannable."
    },
    {
      "reviewer": "Reviewer 2",
      "strongest": "The First Principles Thinker (B) correctly identifies that the skill's core job is answering three questions: what code, what headers, what should the client do. This framework exposes the real structural problem — the reference files use different organizational patterns, so the agent can't build a consistent mental model. Standardizing around the three-question framework would improve every reference file.",
      "biggest_blind_spot": "The Contrarian (A) is right about eval quality but wrong about the Content Ownership section. Telling agents what a skill doesn't own is genuinely useful — it prevents the agent from asking this skill to configure Django middleware or design project-specific compatibility policies. The negative framing is fine; agents need boundaries, not just capabilities.",
      "what_all_missed": "No one mentioned that the skill has no guidance on status codes for streaming responses (like SSE or chunked transfer). 100 Continue is mentioned in rare-and-nonstandard.md, but the broader question of 'what status code does a streaming endpoint return if the stream fails mid-flight?' is unaddressed. This is increasingly relevant for AI/LLM APIs."
    },
    {
      "reviewer": "Reviewer 3",
      "strongest": "The Executor (E) again — the five-step improvement plan is actionable and correctly prioritized. The suggestion to split Eval 1 into two focused evals is particularly good. The current Eval 1 is a kitchen-sink that doesn't discriminate between an agent that understands the basics and one that only handles the easy cases.",
      "biggest_blind_spot": "The Outsider (D) raises valid UX concerns but overweights the 'cold read' problem. Skills aren't meant to be read cold by agents unfamiliar with the domain — they're triggered by task context. The description's code pairs are trigger keywords, not educational content. That said, the dual routing (Quick Start + Task Routing) is genuinely confusing and should be fixed.",
      "what_all_missed": "None of the advisors questioned whether the reference file structure is optimal. There are 6 files (5 references + 1 index) for what is fundamentally a lookup task. Could the content be consolidated into 3 files without losing the 'read only what you need' benefit? Fewer files means less navigation overhead and less risk of cross-file inconsistency."
    },
    {
      "reviewer": "Reviewer 4",
      "strongest": "The First Principles Thinker (B) and The Executor (E) together identify the two most critical issues: inconsistent reference file structure and bloated SKILL.md. If I had to pick one, I'd go with B's insight about the three-question framework because it's the diagnostic that leads to the prescription. Once you see that every reference file should answer 'what code, what headers, what should the client do,' the restructuring becomes obvious.",
      "biggest_blind_spot": "The Expansionist (C) suggests adding client-side error handling patterns 'more assertively' but the skill already does this — every decision-rules table has a 'What the client should do' column, and Eval 3 tests exactly this. The skill is already more client-aware than C gives it credit for. The gap isn't client-side guidance; it's that the guidance is scattered across files instead of consolidated.",
      "what_all_missed": "No one addressed the skill's handling of content negotiation failures. 406 Not Acceptable is mentioned briefly in client-errors.md's 'Advanced' table, but there's no guidance on when to use 406 vs just returning JSON regardless of the Accept header (which is what most real APIs do). This is a common design decision point that agents face."
    },
    {
      "reviewer": "Reviewer 5",
      "strongest": "The Contrarian (A) delivers the harshest and most useful critique: the evals test pattern matching, not judgment. Eval 1 is especially guilty — it's a straight lookup exercise. The suggestion to test ambiguous scenarios where two codes are defensible would dramatically improve eval quality. An agent that can argue for 409 vs 422 for a duplicate entry, with tradeoffs, understands HTTP semantics better than one that memorizes the 'correct' answer.",
      "biggest_blind_spot": "The Executor (E) is too conservative. Suggesting that OpenAPI mapping and client SDK patterns are 'scope creep' ignores that this skill is already doing more than pure reference — it includes client guidance, retry matrices, and compatibility notes. Adding a 'Related Skills' section pointing to drf, litestar, or webapp-testing would cost two lines and prevent agents from operating in a vacuum.",
      "what_all_missed": "None of the advisors mentioned that the skill doesn't address HEAD requests and their status code implications. A HEAD request should return the same status code as GET but without a body — this trips up agents who design separate HEAD handlers. A single note in success-redirects.md would suffice."
    }
  ],
  "chairman_verdict": {
    "agrees": "The council broadly agrees on three points: (1) The SKILL.md description is bloated and should be trimmed to a clear, concise purpose statement. (2) The dual routing system (Quick Start + Task Routing) is redundant and confusing — consolidate into one. (3) The evals need improvement — Eval 1 is too broad and tests recall over reasoning. There's also agreement that 207 Multi-Status is a notable omission from the reference files.",
    "clashes": "The council clashes on scope. The Expansionist wants to add OpenAPI mapping, API versioning, versioning guidance, and end-to-end client SDK patterns. The Executor calls this scope creep and wants to keep the skill focused on code selection. The Contrarian and Outsider disagree on whether the Content Ownership section is useful boundary-setting or defensive scope-lawyering. The First Principles Thinker and Executor disagree on whether the reference file count should be reduced — B implies consolidation would help, while E's fixes assume the current file structure.",
    "blind_spots": "The entire council missed three things: (1) inconsistent presentation format across reference files (tables vs prose vs matrices), which forces agents to re-learn the structure for each file; (2) no mention of HEAD request semantics or streaming response status codes; (3) no guidance on content negotiation decisions (406 vs ignoring Accept header), which is a real design choice agents face. Reviewer 1 caught the format inconsistency, and Reviewer 2 caught the streaming gap, but no advisor raised these.",
    "recommendation": "Execute the Executor's five-step plan as the immediate improvement: trim the description, consolidate routing, add 207, split Eval 1, and add a Common Mistakes callout. Then apply the First Principles Thinker's three-question framework to standardize all reference files into a consistent table format (code → when to use → required headers → client action). Finally, replace Eval 1 with two focused evals and add one new eval that tests ambiguous scenarios where multiple codes are defensible. Defer OpenAPI mapping, versioning, and SDK patterns to a future skill or a 'Related Skills' section.",
    "first_step": "Rewrite the SKILL.md description to a concise 40-word purpose statement, remove the Quick Start section (keep Task Routing), and add a 'Common Mistakes' callout box with the top 3 anti-patterns. This is a 30-minute change that improves every agent interaction with the skill."
  }
}

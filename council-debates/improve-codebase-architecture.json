{
  "skill_name": "improve-codebase-architecture",
  "skill_path": "/home/minder/projekty/oh-my-slop/skills/improve-codebase-architecture/SKILL.md",
  "framed_question": "How can the improve-codebase-architecture skill be improved? What's missing, weak, redundant, or could be better structured? Consider: clarity, completeness, triggering accuracy, examples, edge cases, anti-patterns, organization, and real-world effectiveness.",
  "advisors": [
    {
      "name": "The Contrarian",
      "response": "This skill has a serious identity crisis. It bills itself as an architecture review tool but is really a report-generation exercise dressed up in deep-module vocabulary. The entire value proposition hinges on producing a flashy HTML file — but the HTML is a delivery mechanism, not the skill. The actual architectural reasoning is outsourced to the codebase-design skill, meaning this skill is a thin wrapper around another skill plus a template. That's not a skill, it's a script.\n\nThe grilling loop (step 3) is embarrassingly vague. It says 'use court-jester for adversarial review OR conduct a collaborative specification interview.' Pick one. Giving the agent two contradictory modes with no routing logic means it'll default to whichever is easier — probably the collaborative interview, which is just a friendly chat that won't surface real risks. The skill needs to mandate the adversarial pass first, then offer collaboration as a follow-up.\n\nThe HTML report requirement is over-engineered. Mandating Tailwind CDN, Mermaid CDN, hand-built SVG, cross-sections, mass diagrams — for what? The user asked for architectural advice, not a design portfolio piece. This is busywork that inflates token usage and execution time without proportional value. A structured text report with clear before/after descriptions would be faster, more accessible, and equally actionable. The skill should offer HTML as an option, not mandate it.\n\nThe evals are toothless. Eval 1 checks that the agent 'proposes writing an HTML report' — that's a checkbox, not a quality bar. There's no eval for whether the architectural analysis is actually correct, whether the deletion test is applied properly, or whether the before/after diagrams are accurate. You're testing the process, not the output.\n\nFinally, the skill has no termination criteria. When does the grilling loop end? How many candidates should be surfaced? What if the codebase is 10 lines? What if it's 500K lines? There's no scoping guidance, no time/effort budget, no 'this skill is not appropriate for X' section."
    },
    {
      "name": "The First Principles Thinker",
      "response": "Strip away the vocabulary and the HTML scaffolding. What is this skill actually trying to do? It wants to help a developer find the highest-leverage refactoring in their codebase — the one change that makes everything else easier. That's the core job.\n\nFrom first principles, that requires three things: (1) a way to identify candidates, (2) a way to evaluate their impact, and (3) a way to validate the proposed change before committing. The skill has #1 (explore for friction) and a weak #2 (deletion test, depth vocabulary), but #3 is where it falls apart. The grilling loop is underspecified — it name-drops court-jester but doesn't define what a 'good' grilling looks like. What questions must be answered? What's the minimum viable specification before a deepening is approved?\n\nThe skill also assumes the codebase has seams to find. But what about greenfield code? Or code that's uniformly shallow with no clear consolidation targets? The skill needs an explicit 'no candidates found' path — a way to say 'your architecture is either already deep or the problems are at a different layer (data model, deployment topology) that this skill doesn't address.'\n\nThe dependency on codebase-design is architecturally sound (don't duplicate the vocabulary), but the skill never tells the agent to actually load that skill. It says 'use the codebase-design skill' in prose but doesn't include a `skill: codebase-design` directive or any mechanism to ensure the vocabulary is loaded. In practice, the agent will either already have it (lucky) or won't (silent vocabulary drift).\n\nThe HTML report is a presentation layer concern. The real artifact should be the architectural decision — the before/after model, the test strategy, the risk assessment. The HTML is just how you show it. The skill should separate the analysis from the presentation, making the analysis the primary artifact and the report a secondary rendering."
    },
    {
      "name": "The Expansionist",
      "response": "This skill is sitting on a much bigger opportunity than it realizes. Right now it's a one-shot architecture review. But the real value is in making it a living practice — a recurring architectural health check that tracks deepening over time.\n\nImagine: the skill stores a snapshot of the architecture review (the candidates, the chosen deepening, the before/after state) in a project-level file or ADR. Next time it runs, it compares against the last snapshot. Did the deepening actually happen? Did it create new shallow modules? Are there new candidates that emerged from the last refactor? This turns a point-in-time review into an architectural evolution tracker.\n\nThe HTML report could be versioned and diffed. Show the user: 'Last time, you had 3 shallow modules in the Order pipeline. Now you have 1 deep module and 2 remaining candidates.' That's a progress bar for architecture quality — incredibly motivating and useful for team leads.\n\nThe skill could also integrate with CI. A lightweight mode that runs on every PR, checks for new shallow modules being introduced, and flags them in the PR review. Not the full HTML report — just a 'hey, this new module has a 1:1 interface-to-implementation ratio, consider deepening' comment.\n\nThe grilling loop could produce a structured decision record — not just a conversation, but a mini-ADR with the thesis, the counter-arguments, the decision, and the expected outcomes. This feeds directly into the project's ADR log and gives the skill lasting impact beyond the session.\n\nAnd the vocabulary enforcement — don't use 'component,' use 'module' — this is actually a linting opportunity. The skill could include a vocabulary checker that scans the codebase for inconsistent terminology and suggests alignment. That's a whole adjacent skill hiding inside this one."
    },
    {
      "name": "The Outsider",
      "response": "I just read this skill and I'm confused about when I'm supposed to use it. The description says 'scan a codebase for deepening opportunities' — but what does 'deepening' mean to someone who hasn't read the codebase-design skill? The description doesn't explain the concept. It name-drops 'deep modules' and 'shallow modules' without defining them. A user who doesn't already know this vocabulary will read the description and think 'sure, I want to improve my architecture' without understanding what specific thing this skill does versus, say, a general code review.\n\nThe process section jumps straight into 'Explore' with no setup. What codebase? The current directory? A path I specify? A git repo URL? The skill doesn't tell me what input it expects. I'm an agent — I need to know what to read, where to look, what the scope is.\n\nThe HTML report section is detailed about styling (Tailwind CDN, Mermaid, hand-built SVG) but vague about content. What makes a good candidate? How many should I surface? What if I find 20 candidates — do I list all 20? The skill says 'present candidates' but doesn't define the expected count or prioritization method.\n\nThe grilling loop says 'once the user picks a candidate, conduct a specification interview to walk the design tree.' What's a design tree? What questions do I ask? The skill references court-jester but doesn't say whether court-jester replaces the interview or supplements it. I'm left guessing at the workflow.\n\nThe reference index at the bottom is just a table pointing to html-report.md. That's it. No examples of what a good output looks like, no sample candidate card, no before/after of a real (even toy) architecture review. I'd need to reverse-engineer the expected quality from the HTML scaffold, which is a styling guide, not a content guide.\n\nAlso: the skill says 'Do NOT propose interfaces yet' in step 2, but doesn't say when to propose them. Step 3 mentions 'the shape of the deepened module' but doesn't give a template or checklist for what a good interface proposal looks like."
    },
    {
      "name": "The Executor",
      "response": "The fastest path to making this skill actually useful is to fix three things:\n\n**1. Add a concrete example.** The single biggest gap is that there's no worked example of a candidate card — not even a toy one. The HTML report reference has scaffold code and diagram patterns, but no example of a filled-in card with real (even fictional) module names, a real problem statement, and a real before/after. Add one complete example card to the html-report.md reference. That alone will improve agent output quality by 50%.\n\n**2. Define the grilling loop as a concrete checklist.** Replace 'conduct a specification interview to walk the design tree' with 5-7 specific questions the agent must answer: What's behind the seam? What tests exist today? What tests would exist after? What breaks if this deepening is wrong? What's the rollback plan? This turns vague guidance into executable steps.\n\n**3. Add scope boundaries.** One line: 'For codebases under 500 lines, skip the HTML report and present candidates inline. For codebases over 100K lines, focus on one package/module per run.' This prevents the agent from either over-engineering a tiny review or drowning in a massive codebase.\n\nEverything else — the HTML styling, the Mermaid vs. hand-built diagram guidance, the vocabulary enforcement — is fine as-is. The skill's structure is sound. It just needs concrete examples and executable checklists to go from 'sounds good in theory' to 'works in practice.'\n\nThe evals also need one more case: an eval where the codebase has no good deepening candidates, to test that the skill can say 'nothing to do here' gracefully. That's the second-fastest win."
    }
  ],
  "peer_reviews": [
    {
      "reviewer": "Reviewer 1",
      "strongest": "The Executor — it identifies the highest-leverage fixes with minimal fuss. The 'add a concrete example' recommendation is the single most impactful change possible. Agents learn from examples ten times faster than from abstract guidance. The checklist for the grilling loop is equally practical.",
      "biggest_blind_spot": "The Expansionist — the CI integration and architectural evolution tracker ideas are exciting but completely out of scope for a skill file. You're describing a product feature, not a skill improvement. The vocabulary linting idea is interesting but belongs in a separate skill.",
      "what_all_missed": "None of the advisors addressed the skill's description field — the YAML frontmatter `description` that controls triggering. It says 'Scan a codebase for deepening opportunities, present them as a visual HTML report, then grill through whichever one you pick.' This is vague and doesn't mention the vocabulary dependency, the deletion test, or the 3-phase process. A better description would improve triggering accuracy significantly."
    },
    {
      "reviewer": "Reviewer 2",
      "strongest": "The First Principles Thinker — correctly identifies that the skill's core job is finding the highest-leverage refactoring, and that the three required capabilities are identification, evaluation, and validation. The observation that the skill never actually instructs the agent to load the codebase-design skill is a critical catch that nobody else made.",
      "biggest_blind_spot": "The Contrarian — calling the HTML report 'busywork' is wrong. The visual format is the skill's differentiator. Text-only architectural reviews are a dime a dozen; the HTML report with before/after diagrams is what makes this skill worth having. Dumbing it down to 'structured text' would make it generic.",
      "what_all_missed": "The skill has no guidance on what to do when the user's codebase uses a different architectural pattern than deep modules. Event-driven architectures, microservices, and functional pipelines may not map cleanly to the deep-module model. The skill needs a 'when not to use this' section or at least guidance on adapting the vocabulary to different paradigms."
    },
    {
      "reviewer": "Reviewer 3",
      "strongest": "The Outsider — the observation that the skill doesn't explain what input it expects (what codebase? what path?) is fundamental. A skill that doesn't tell the agent what to read is like a function with no parameters. The point about 'no worked example' reinforces The Executor's recommendation but from a different angle — the Outsider shows that even understanding the expected output is impossible without an example.",
      "biggest_blind_spot": "The Expansionist — versioning HTML reports and comparing snapshots is a nice idea but practically infeasible. HTML files in /tmp are ephemeral by design. The skill explicitly writes to temp directories to avoid repo pollution. You can't diff files that get deleted on reboot. This suggestion contradicts the skill's own file hygiene rules.",
      "what_all_missed": "The skill references `codebase-design/SKILL.md` and `court-jester/SKILL.md` using relative paths (`../codebase-design/SKILL.md`). If this skill is ever invoked from a different directory structure (e.g., a user's local skills directory that doesn't have these siblings), the references break. The skill should either use absolute paths or include a fallback instruction."
    },
    {
      "reviewer": "Reviewer 4",
      "strongest": "The Executor — the recommendation to add scope boundaries (under 500 lines → inline, over 100K lines → one package per run) is the kind of practical guardrail that separates skills that work from skills that flounder. The eval for 'no candidates found' is equally important — every skill needs a graceful negative path.",
      "biggest_blind_spot": "The Contrarian — dismissing the entire skill as 'a thin wrapper around another skill plus a template' is reductive. Many valuable skills are thin wrappers that orchestrate other skills. The value is in the process (explore → present → grill), not in the individual components. The skill's orchestration is its contribution.",
      "what_all_missed": "The skill says to use `$TMPDIR` falling back to `/tmp` (or `%TEMP%` on Windows), but the HTML report reference doesn't mention this at all — it just says 'write to the OS temp directory.' The two references should be consistent. Also, the skill doesn't address what happens if `xdg-open`/`open`/`start` fails — there should be a fallback instruction like 'if the open command fails, print the path and instruct the user to open it manually.'"
    },
    {
      "reviewer": "Reviewer 5",
      "strongest": "The First Principles Thinker — the identification of the missing 'no candidates found' path is the most architecturally significant gap in the skill. A tool that always finds problems is a tool that can't be trusted. The skill needs to be able to say 'your architecture is already deep' or 'the problems are at a different layer' with the same confidence it says 'here's what to fix.'",
      "biggest_blind_spot": "The Outsider — while the 'what codebase?' question is valid, it's also the kind of thing that's typically handled by the agent's context (current working directory, project root). Most skills don't explicitly state 'read the current directory' — it's implied. This is a minor clarity issue, not a fundamental gap.",
      "what_all_missed": "The skill's grilling loop says to 'use the court-jester skill for adversarial review of the proposed deepening, or conduct a collaborative specification interview.' The word 'or' creates ambiguity about when to use which. The skill should define a decision rule: use court-jester when the user is confident in their choice and needs stress-testing; use the interview when the user is uncertain and needs to explore. Without this routing logic, the agent will guess."
    }
  ],
  "chairman_verdict": {
    "agrees": "The council broadly agrees on three points: (1) the skill needs a concrete worked example — at least one complete candidate card with fictional but realistic content — to give the agent a quality target; (2) the grilling loop is underspecified and needs a concrete checklist of questions or a defined interview structure; and (3) the skill needs a 'no candidates found' / negative path so it can gracefully handle codebases where deepening isn't the right move. There's also agreement that the skill should explicitly instruct the agent to load the codebase-design skill rather than assuming the vocabulary is available.",
    "clashes": "The council clashes sharply on the HTML report. The Contrarian sees it as over-engineered busywork; the Expansionist sees it as a foundation for a larger architectural tracking system; Reviewer 2 argues it's the skill's key differentiator. The Contrarian also dismisses the skill as a 'thin wrapper,' while Reviewer 4 argues that orchestration is the skill's actual value. On scope: the Executor wants hard line-count boundaries; the Outsider thinks the input scope question is minor and implied by context.",
    "blind_spots": "The entire council missed the frontmatter `description` field's impact on triggering accuracy — it's vague and doesn't mention the vocabulary dependency or the 3-phase process. Nobody addressed the relative path fragility for skill references (`../codebase-design/SKILL.md`). The fallback for failed `xdg-open`/`start` commands was also universally overlooked. Additionally, no advisor discussed what happens when the codebase's architectural paradigm (event-driven, microservices) doesn't map cleanly to the deep-module model.",
    "recommendation": "Prioritize these changes in order: (1) Add one complete worked example to html-report.md — a fictional candidate card with filled-in problem, solution, before/after diagram, and wins. (2) Replace the vague grilling loop with a 5-7 question checklist that constitutes a minimum viable specification. (3) Add a negative path: explicit guidance for when no candidates are found or when the architecture is already deep. (4) Add an explicit instruction to load the codebase-design skill at the start of step 1. (5) Add scope boundaries (inline for small codebases, single-package for large ones). (6) Add an eval for the negative path. (7) Update the frontmatter description to mention the 3-phase process and vocabulary dependency.",
    "first_step": "Add a complete worked example candidate card to references/html-report.md. This is the single highest-leverage change — it clarifies the expected output quality, gives the agent a concrete template to follow, and resolves the 'no example' gap identified by both The Executor and The Outsider. Everything else builds on having a clear target."
  }
}

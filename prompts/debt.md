---
description: Find all SHORTCUT markers in the codebase
argument-hint: "[path] [--output-debt-file]"
---
Use the `ponytail-debt` skill to harvest `SHORTCUT:` markers from the codebase at `${1:-.}`, with options `$2`. If it isn't among your available skills, locate its `SKILL.md` in the installed `oh-my-slop` package and follow that.

---
description: Generate an interactive bash wizard that walks a human through a manual setup or migration procedure
argument-hint: "[description]"
---
Use the `wizard` skill to generate a wizard for: ${1:-.}. If it isn't among your available skills, locate its `SKILL.md` in the installed `oh-my-slop` package and follow that.

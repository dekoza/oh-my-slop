---
description: Architecture health check with HTML report
argument-hint: "[path]"
---
Use the `improve-codebase-architecture` skill to review the codebase at `${1:-.}`. If it isn't among your available skills, locate its `SKILL.md` in the installed `oh-my-slop` package and follow that.
